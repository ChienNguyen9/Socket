// Chien Nguyen
// PS# 1328764

// SERVER

#include <iostream>
#include <string>
#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <unistd.h>

using namespace std;

int main() {

  // Create a socket

  // Bind an address to that socket

  // Set up the buffer size for the socket

  // Wait for incoming calls

  // Accept incoming calls (and get a new socket)

  // Reply with the requested public key

  // Hang up

  // Repeat steps 4 and 7


  return 0;
}
